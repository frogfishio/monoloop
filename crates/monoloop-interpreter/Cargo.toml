[package]
name = "monoloop-interpreter"
version.workspace = true
edition.workspace = true
license.workspace = true
rust-version.workspace = true
description = "Async incremental dialect interpreter and canonical-unit assembler"

[dependencies]
bytes = { workspace = true }
monoloop-contracts = { path = "../monoloop-contracts" }
serde = { workspace = true }
serde_json = { workspace = true }
tokio = { workspace = true }
tracing = { workspace = true }
uuid = { workspace = true }

[dev-dependencies]
tokio = { workspace = true, features = ["test-util", "macros", "rt-multi-thread", "sync", "time"] }
